// Static viewer: winit + wgpu, families as sized glyphs at computed positions,
// edges as metadata-colored bridges, free-camera navigation.
// See DESIGN.md §Build order (main line 4) and §First executable milestone.
fn main() {
    todo!("static viewer: winit + wgpu, free-camera navigation over 6561 family glyphs")
}
